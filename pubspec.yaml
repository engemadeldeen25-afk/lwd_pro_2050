name: lwd_app
description: LWD testing application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.35.5
  fl_chart: ^0.63.0
  csv: ^5.1.1
  permission_handler: ^11.3.1
  intl: ^0.18.1
  geolocator: ^11.0.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true